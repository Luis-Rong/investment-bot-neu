"""MCP (Model Context Protocol) server package.

Exposes the bot's data / risk / RAG capabilities as a standardized MCP service
so they can be consumed by any MCP client (Claude Desktop, another agent, an
IDE) — not just from inside this app. See `mcp_server/server.py`.
"""
